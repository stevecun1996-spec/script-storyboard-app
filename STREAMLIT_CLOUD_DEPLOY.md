# Streamlit Cloud 部署指南

## 🚀 快速部署步骤

### 步骤 1：准备 GitHub 仓库

#### 1.1 初始化 Git 仓库

```bash
cd "/Users/Zhuanz/Desktop/cursor 工程/剧本到分镜-github/Script_to_Storyboard_Simple-main/Script_to_Storyboard_Simple"
git init
git add .
git commit -m "Initial commit: 剧本分镜生成系统"
```

#### 1.2 创建 GitHub 仓库

1. 访问：https://github.com/new
2. 填写仓库名称（例如：`script-storyboard-app`）
3. 选择 **Public** 或 **Private**（Public 可以免费使用 Streamlit Cloud）
4. **不要**勾选 "Initialize this repository with a README"
5. 点击 "Create repository"

#### 1.3 连接并推送代码

```bash
# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送代码
git branch -M main
git push -u origin main
```

---

### 步骤 2：部署到 Streamlit Cloud

#### 2.1 访问 Streamlit Cloud

1. 访问：https://share.streamlit.io/
2. 点击右上角 **"Sign in"**
3. 选择 **"Continue with GitHub"**
4. 授权 Streamlit Cloud 访问你的 GitHub 账号

#### 2.2 创建新应用

1. 点击 **"New app"** 按钮
2. 填写部署信息：
   - **Repository**: 选择你刚刚创建的仓库
   - **Branch**: `main`（或你的主分支名）
   - **Main file path**: `app.py` ⚠️ **注意：这里只需要写 `app.py`，不是完整路径**
   - **App URL**: 可以自定义应用名称（如：`script-storyboard`）

3. 点击 **"Deploy"**

#### 2.3 等待部署

- Streamlit Cloud 会自动安装依赖
- 部署过程可能需要 2-5 分钟
- 可以在日志中查看部署进度

---

### 步骤 3：配置应用

#### 3.1 设置 Secrets（环境变量）

如果需要配置访问密码，在 Streamlit Cloud：

1. 进入应用的 **"Settings"**（右上角 ⚙️）
2. 找到 **"Secrets"** 选项
3. 点击 **"Edit secrets"**
4. 添加以下内容（根据需要）：

```toml
# 访问密码（如果需要密码保护）
APP_PASSWORD = "你的密码"

# API Keys（可选，如果需要预设）
# OPENAI_API_KEY = "your-key"
# DEEPSEEK_API_KEY = "your-key"
```

5. 点击 **"Save"**

#### 3.2 启用密码保护（可选）

如果需要密码保护：

1. 编辑 `app.py`
2. 取消注释第 22-42 行的密码验证代码
3. 提交并推送代码：
   ```bash
   git add app.py
   git commit -m "Enable password protection"
   git push
   ```
4. Streamlit Cloud 会自动重新部署

---

### 步骤 4：测试应用

1. 部署完成后，访问你的应用 URL
2. 测试所有功能：
   - 输入剧本
   - 配置 API Key
   - 生成分镜
   - 编辑分镜
   - 生成提示词
   - 导出功能

---

## 📋 部署检查清单

### ✅ 部署前检查

- [ ] 所有代码文件已提交到 Git
- [ ] `requirements.txt` 包含所有依赖
- [ ] `.gitignore` 已配置（排除敏感文件）
- [ ] 代码中没有硬编码的 API Key
- [ ] 已创建 GitHub 仓库并推送代码
- [ ] 检查应用主文件路径是否正确（`app.py`）

### ✅ 部署后检查

- [ ] 应用可以正常访问
- [ ] 所有页面正常加载
- [ ] 侧边栏功能正常
- [ ] API 调用正常（如果已配置）
- [ ] 文件管理功能正常
- [ ] 导出功能正常

---

## 🔧 常见问题

### Q1: 部署失败，提示找不到 `app.py`

**A**: 检查 Main file path 配置：
- 如果 `app.py` 在根目录：填写 `app.py`
- 如果 `app.py` 在子目录：填写 `Script_to_Storyboard_Simple/app.py`

### Q2: 依赖安装失败

**A**: 检查 `requirements.txt`：
- 确保所有依赖都列出
- 版本号要兼容
- 可以尝试简化版本要求

### Q3: 应用启动后显示错误

**A**: 查看日志：
- 在 Streamlit Cloud 应用页面点击 "Manage app"
- 查看 "Logs" 标签页
- 根据错误信息修复代码

### Q4: 如何更新应用？

**A**: 推送代码到 GitHub：
```bash
git add .
git commit -m "Update description"
git push
```
Streamlit Cloud 会自动检测并重新部署。

### Q5: 如何查看访问日志？

**A**: 在 Streamlit Cloud：
- 进入应用 Settings
- 查看 "Usage statistics"（使用统计）

---

## 📱 分享应用

### 分享方式

1. **直接分享链接**
   ```
   https://你的应用名.streamlit.app
   ```

2. **分享给朋友**
   - 发送应用链接
   - 如果有密码，一并提供
   - 建议朋友使用自己的 API Key

### 隐私说明

⚠️ **注意**：
- Streamlit Cloud 免费版应用是公开的
- 任何获得链接的人都可以访问
- 建议使用密码保护（已提供代码）
- 建议告知朋友数据存储情况

---

## 🎯 下一步

部署完成后：

1. ✅ 测试所有功能
2. ✅ 配置 Secrets（如需要）
3. ✅ 启用密码保护（如需要）
4. ✅ 分享给朋友使用
5. ✅ 定期检查应用状态

---

## 📞 需要帮助？

- Streamlit Cloud 文档：https://docs.streamlit.io/streamlit-community-cloud
- Streamlit 社区：https://discuss.streamlit.io/
- GitHub Issues：https://github.com/streamlit/streamlit/issues

---

**祝你部署顺利！** 🎉

